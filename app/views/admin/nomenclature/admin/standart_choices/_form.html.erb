<%= semantic_form_for [:admin, @standart_choice]  do |f| %>
  <%= f.error_messages %>

  <%= f.inputs do %>
    <%= f.input :locale, :value => I18n.locale, :as => :hidden %>
    <%= f.input :active, :as => :boolean %>
    <%= f.input :name %>
  <% end %>

  <p>
    <%= f.submit t(:create) %> |
    <%= link_to t("index", :scope => [:admin, :standart_choices, :form]), admin_fence_types_path %>
  </p>

<% end %>
