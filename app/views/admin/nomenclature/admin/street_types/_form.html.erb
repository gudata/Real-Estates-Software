<%= semantic_form_for [:admin, @street_type]  do |f| %>
  <%= f.error_messages %>

  <%= f.inputs do %>
    <%= f.input :active, :as => :boolean %>
    <%= f.input :name %>
    <%= f.input :value %>
  <% end %>

  <p>
    <%= f.submit t(:create) %> |
    <%= link_to t("index", :scope => [:admin, :street_types, :form]), admin_street_types_path %>
  </p>

<% end %>
