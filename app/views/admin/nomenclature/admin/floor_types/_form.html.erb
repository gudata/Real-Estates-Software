<%= semantic_form_for [:admin, @floor_type]  do |f| %>
  <%= f.error_messages %>

  <%= f.inputs do %>
    <%# f.input :locale, :value => I18n.locale, :as => :hidden %>
    <%= f.input :active, :as => :boolean %>
    <%= f.input :name %>
    <%= f.input :value %>
  <% end %>

  <p>
    <%= f.submit t(:create) %> |
    <%= link_to t("index", :scope => [:admin, :floor_types, :form]), admin_floor_types_path %>
  </p>

<% end %>
