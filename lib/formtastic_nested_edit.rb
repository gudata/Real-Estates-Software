module Formtastic
  module NestedEdit
      
    protected
    def nested_editz(method, options = {})
      #        string_input(method, calendar_options(format, object.send(method)).merge(options))
    end

  end
end